#pragma once
/*StatUtilities.h is the file defining statistic functions*/
#include <vector>
std::vector <double> Normalize(std::vector <double> input);
double StdDeviation(std::vector <double> input);
